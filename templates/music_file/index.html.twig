{% extends 'base.html.twig' %}

{% block title %} Hello {{ controller_name }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ 'src/Controller/MusicFileController.php'|file_link(0) }}">src/Controller/MusicFileController.php</a></code>
            </li>
            <li>Your template at <code><a href="{{ 'templates/music_file/index.html.twig'|file_link(0) }}">templates/music_file/index.html.twig</a></code>
            </li>
        </ul>
    </div>
    {% for music in musiques %}
        <p>{{ music.filename }}</p>
        <p>{{ music.fileartist }}</p>
        {{ music.filedate|date('d-m-Y') }}
    {% endfor %}

    <div id="app">ici
        <ol>
            <todo-item></todo-item>
        </ol>
    </div>
{% endblock %}

    {% block javascripts %}
        {{ parent() }}
        <script src="{{ asset('build/viewmusicfile.js') }}"></script>
    {% endblock %}